#!/usr/bin/env sh
set -eu

script_path="$(realpath "$(dirname "$0")/../src/bin/copy-schema.mts")"

bin_dir="$(realpath "$(dirname "$0")")"
tsx_path="$(realpath "$bin_dir/../node_modules/.bin/tsx")"

"$tsx_path" "$script_path" "$@"